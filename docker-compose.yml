version: "3.9"

services:
  backend:
    build: ./backend
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    environment:
      # Supabase Configuration
      SUPABASE_URL: ${SUPABASE_URL:-}
      SUPABASE_KEY: ${SUPABASE_KEY:-}
      SUPABASE_SERVICE_ROLE_KEY: ${SUPABASE_SERVICE_ROLE_KEY:-}
      
      # Database Configuration (Supabase)
      DATABASE_URL: ${DATABASE_URL:-}
      
      # OpenAI Configuration
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      
      # Application Configuration
      DEBUG: ${DEBUG:-True}
      ENVIRONMENT: ${ENVIRONMENT:-development}
    ports: ["8000:8000"]
    networks: [eventnet]
    restart: unless-stopped


  frontend:
    build: ./frontend
    environment:
      NEXT_PUBLIC_API_URL: http://139.180.144.194:8000
      NEXT_PUBLIC_HOST_URL: http://139.180.144.194:3000
    depends_on: [backend]
    ports: ["3000:3000"]
    networks: [eventnet]
    restart: unless-stopped


# REMOVE the entire 'event-db' service block:
# event-db:
#   image: postgres:16-alpine
#   ... (etc.)

# REMOVE the 'depends_on' condition from the backend service:
#   depends_on:
#     event-db:
#       condition: service_healthy

# REMOVE the 'volumes' block if 'db_data' is no longer needed
# volumes:
#   db_data:

networks:
  eventnet: {}

# REMOVE the 'volumes' block since 'event-db' is gone
# volumes:
#   db_data: {}